## Remediation cycles (2026-09-02)

| Slice                                  | RED command/result                                                                                                                                                                                          | GREEN command/result                                                                                                                                                    |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Bounded per-run stream lifecycle       | `go test ./internal/speedtest -run TestRunStreamLimitExpiryAndDeterministicRelease -count=1` failed because stream-aware lifecycle APIs did not exist.                                                      | The same test passed with deadline-owned contexts, semaphore admission, release, and expiry reclamation.                                                                |
| Adaptive browser measurement           | `npm test -- --run src/features/speed/engine.test.ts` failed because `runAdaptive` did not exist.                                                                                                           | The same test passed with the 1→2→4→8 engine and server-ACK result model.                                                                                               |
| Backend regression suite               | Existing tests could not exercise new stream-security semantics.                                                                                                                                            | `go vet ./...`, `go test ./...`, and `go test -race ./...` passed.                                                                                                      |
| Frontend compile/build                 | `npm run typecheck` found an inferred `never[]` upload sample type.                                                                                                                                         | Typecheck and build passed after adding the explicit upload phase type.                                                                                                 |
| Embedded asset smoke                   | The hard-coded `/assets/index.js` did not exist in Vite output.                                                                                                                                             | A built binary returned 200 for `/`, its emitted hashed JS asset, and `/healthz` with security headers.                                                                 |
| Browser transport                      | `npm test -- --run src/features/speed/protocol.test.ts` failed because `./protocol` did not exist.                                                                                                          | The same test passed with parallel download byte accounting and server-acknowledged upload chunks.                                                                      |
| Stability chart filtering              | Chart tests had no adapter boundary.                                                                                                                                                                        | The focused chart test passed; 10m filtering uses a non-mutating copy.                                                                                                  |
| Application delivery                   | Playwright started the built-Go server but the pinned local Chromium binary was unavailable.                                                                                                                | Playwright discovered 10 tests across Chromium, Firefox, WebKit, and mobile projects; execution is delegated to Forgejo CI.                                             |
| Owned run reclamation                  | `TestCloseOwnedRestoresCapacityOnlyForOwner` failed because `Registry.CloseOwned` did not exist.                                                                                                            | The same test passed with atomic owner validation, removal, cancellation, and capacity restoration.                                                                     |
| Browser run cleanup                    | `App.test.tsx` failed because `closeRun` did not exist and then because it did not report terminal success.                                                                                                 | It passed with keepalive `DELETE`, pre-result reclamation, `finally` retry, and TTL fail-safe.                                                                          |
| Browser-independent direction deadline | The engine test showed browser work that ignored `AbortSignal` never settled.                                                                                                                               | It passed after each direction raced work against its own timeout/cancellation stop promise.                                                                            |
| Buffered download wall clock           | Protocol tests first drained more than ten buffered chunks, then showed a WebKit-like `reader.read()` that never settled.                                                                                   | They passed after per-chunk elapsed checks plus a phase-stop promise raced both fetch and every reader operation at the 1.25-second boundary.                           |
| Forgejo CI and OCI caches              | `go test ./tests/contracts -run 'Test(CICache\|ContainerWorkflow)' -count=1` failed because pinned CI caching and OCI cache import were absent; monolithic cache post-save was also skipped on E2E failure. | It passed with immutable Forgejo `cache/restore` and `cache/save` actions around setup, versioned cache paths, and credential-free best-effort OCI inline-cache import. |
| WebKit PNG download activation         | `App.test.tsx` first failed because `downloadBlob` did not exist, then because the anchor was removed immediately after click.                                                                              | It passed after retaining the connected anchor and object URL for a 1-second WebKit download-adoption window, then removing and revoking both deterministically.        |

External validation is required for each revision because local Playwright binaries and a Docker daemon are unavailable. Forgejo CI provides multi-engine E2E, container smoke, registry push/pull, and digest evidence.
