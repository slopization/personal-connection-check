## Remediation cycles (2026-09-02)

| Slice                                  | RED command/result                                                                                                                                                                                                                  | GREEN command/result                                                                                                                                                                                                     |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Bounded per-run stream lifecycle       | `PATH="$PWD/.tools/go/bin:$PATH" go test ./internal/speedtest -run TestRunStreamLimitExpiryAndDeterministicRelease -count=1` — failed to compile because `Run.Acquire`, `Release`, `Context`, and stream-aware `New` did not exist. | Same test passed after deadline-owned run contexts, semaphore admission, idempotent release, and expiry reclamation were implemented.                                                                                    |
| Adaptive browser measurement           | `cd web && npm test -- --run src/features/speed/engine.test.ts` — failed: `runAdaptive is not a function`.                                                                                                                          | Same test passed after the 1→2→4→8 transport engine and server-ack result model were added.                                                                                                                              |
| Backend integration regression suite   | Existing narrow suite could not exercise new stream-security semantics.                                                                                                                                                             | `PATH="$PWD/.tools/go/bin:$PATH" go vet ./... && PATH="$PWD/.tools/go/bin:$PATH" go test ./... -count=1 && PATH="$PWD/.tools/go/bin:$PATH" go test -race ./... -count=1` passed.                                         |
| Frontend compile/build                 | First `npm run typecheck` after the typed UI replacement failed on an inferred `never[]` upload sample type.                                                                                                                        | `npm run typecheck && npm run build` passed after an explicit upload phase type was introduced.                                                                                                                          |
| Embedded asset HTTP smoke              | The original hard-coded `/assets/index.js` did not exist in Vite output.                                                                                                                                                            | A running built binary returned 200 for `/`, the actual emitted hashed JS reference, and `/healthz`; CSP/nosniff headers were observed.                                                                                  |
| Browser transport                      | `cd web && npm test -- --run src/features/speed/protocol.test.ts` — RED: failed to resolve `./protocol`.                                                                                                                            | GREEN: the same focused test passed after real parallel `ReadableStream` download byte accounting and repeated server-acknowledged upload chunks were implemented.                                                       |
| Stability chart filtering              | Chart tests previously had no adapter boundary.                                                                                                                                                                                     | `cd web && npm test -- --run src/features/stability/StabilityChart.test.ts` passed; 10m filtering uses a non-mutating filtered copy.                                                                                     |
| Application delivery                   | `./node_modules/.bin/playwright test --project=chromium --reporter=line` started the built-Go test server and discovered both real-browser flows, then RED because the pinned Chromium executable is absent locally.                | `./node_modules/.bin/playwright test --list` GREEN-discovered 10 tests over Chromium, Firefox, WebKit and mobile projects. CI runs `make e2e-install` followed by `make e2e` using the lockfile-pinned local binary.     |
| Owned run reclamation                  | `PATH="$PWD/.tools/go/bin:$PATH" go test ./internal/speedtest -run TestCloseOwnedRestoresCapacityOnlyForOwner -count=1` — failed to compile because `Registry.CloseOwned` did not exist.                                            | Same test passed after atomic owner validation, removal, cancellation, and immediate capacity restoration were implemented.                                                                                              |
| Browser run cleanup                    | `cd web && npm test -- --run src/app/App.test.tsx` — failed because `closeRun` did not exist, then failed because it did not report terminal cleanup success.                                                                       | Same test passed after a keepalive `DELETE` helper was added; successful measurements reclaim capacity before presenting results, while abort/error paths retry cleanup in `finally` and retain server TTL as fail-safe. |
| Browser-independent direction deadline | `cd web && npm test -- --run src/features/speed/engine.test.ts` — `finishes both direction deadlines when browser work ignores abort` failed because a transport promise that ignored `AbortSignal` never settled.                  | Same test passed after each direction raced browser work against its own rejecting stop promise; timeout and caller cancellation now settle independently of WebKit fetch/stream abort behavior.                         |

External-only validation still required for each new revision: Forgejo runner execution (including registry digest evidence) and Docker daemon image smoke; the local Docker daemon is unavailable. Browser Playwright binaries are not installed locally, so full multi-engine E2E remains CI-runner validation.
